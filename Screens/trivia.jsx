import { StyleSheet, View, Text, Image } from 'react-native'
import React from 'react'

export default function Trivia() {
    return (
        <View>
          <Text>Hello Trivia</Text>
        </View>
    )
}

// Styling
const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        display: 'flex',
    },
});